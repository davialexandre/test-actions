name: Main
on: push
env:
    MY_ENV_VAR: 321

jobs:
    tests:
        runs-on: ubuntu-latest
        container: 
            image: alpine:latest
            volumes:
                - ${{ github.workspace }}:/var/www

        steps:
        
        - uses: actions/checkout@v2

        - name: First step 
          run: |
            env
            export MYVAR=10
            MY_ENV_VAR=321

        - name: Second step
          run: env
            


