name: Main
on: pull_request

env:
    MY_ENV_VAR: 321

jobs:
    tests:
        runs-on: ubuntu-latest

        steps:
        
        - uses: actions/checkout@v2

        - name: First step 
          shell: bash
          run: |
#            env
            git status
            git fetch origin/$GITHUB_BASE_REF
            echo "==============================="
            git remote -v
            echo "==============================="
            git branch -a
            git diff origin/$GITHUB_BASE_REF --name-only -- **/*.css **/*.scss
