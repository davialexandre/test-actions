name: Main
on: push
env:
    MY_ENV_VAR: 321

jobs:
    tests:
        runs-on: ubuntu-latest

        steps:
        
        - uses: actions/checkout@v2

        - name: First step 
          shell: bash
          run: |
            env
            echo ${{ github.ref }}
            git status
            git diff origin/${{ github.ref }}
